const admin = require("../utils/firebase");

// Controller tài chính (financeController)
// Viết các hàm xử lý tài chính tại đây

module.exports = {
  // Thêm các hàm API ở đây, ví dụ:
  // listFinanceLogs: async (req, res) => {},
  // createPaymentRequest: async (req, res) => {},
  // ...
};
